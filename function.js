//opgave 1
function sumArray(numbers) {
    let sum = 0;
    for (let i = 0; i < numbers.length; i++) {
        sum += numbers[i];
    }
    return sum;
}

let tal = [1, 2, 3];
console.log(sumArray(tal));


//opgave 2
function max(numbers)
let 